using Unity.MLAgents.Sensors;

public static class ObserveSelf
{
	// Absolute position (if not included in observeplayer)
	// Absolute eulerY rotation represented in (cos, sin)

	// anyway
	public static void Observe(VectorSensor sensor, Player obs)
	{
	}
}
